# UNKNOWNS — Open Questions & Blocked Decisions

| # | Question | Impact | Owner | Status |
|---|----------|--------|-------|--------|
| 1 | Should the `.tmp/*.skill` portable archives be replaced by a generator script (e.g., a `scripts/pack-skill.sh`) so they can't drift from `skills/`? | Low | framework | Open |
| 2 | Are the new generative skills' "technology tilt" gates too strict for non-tech-host projects (e.g., a pure marketing repo)? | Medium | framework | **Resolved 2026-08-10** — tilt is now conditional: `biz-writing` and `biz-social` priority 4 detect a non-tech host (no stack manifest) and tilt the specificity requirement toward domain evidence from `PROJECTS.md` / `OPERATOR_PROFILE.md` instead of forcing technical detail |
| 3 | Should `biz-writing` carry its own SEO keyword research mode, or always source keywords from `.work.biz/strategy/*`? | Low | framework | Open |
| 4 | Should `biz-social` platform research require live web fetch capability (Reddit API, Instagram), or is pattern-based research sufficient? | Medium | framework | Open — currently uses pattern-based research with explicit "unable to perform live research" fallback |
| 5 | Should `biz-community` split into platform-specific sub-skills if it keeps growing (currently ~280 lines covering 6+ platforms)? | Low | framework | Open — monitor as platform-specific engagement rules grow |
| 6 | Should existing thin/fat adopters get an `@biz-deploy-basic update` / bootstrap migration that creates `reference/CONTENT_STATUS.md` if missing, or only new bootstraps? | Low | framework | **Resolved 2026-08-10** — verified in code: `biz-deploy-basic.sh` runs `templates/bootstrap.sh` in every mode (skip/update/force), and `copy_if_missing` creates `reference/CONTENT_STATUS.md` outright when absent. The merge-candidate scan only covers existing-but-differing files; missing files are created, not merged. Belt-and-braces: content skills now self-heal a missing tracker at record time (CONVENTIONS § Content Status Protocol) |
| 7 | Do the Substack and Threads guides actually hold? Both were written from cited 2026 sources but no project has published to either platform, so nothing is validated against real output | Medium | framework | Open — first Substack publish in `future-strategy` is the earliest test |
| 8 | Is a per-platform tracker file per platform the right shape, or does it fragment the record that `@biz-review` needs to read? Chosen because meaningful metrics differ per platform and a shared schema forces tracking impressions everywhere, the metric least connected to revenue | Medium | framework | Open — revisit after three platforms are tracked in one project |
| 9 | Should skills be forbidden from stating statistics without an inline citation, enforced by `framework-verify`? Two skills shipped uncitable numbers while the standards mandate evidence-first, which suggests prose review is not catching it | Medium | framework | **Resolved 2026-08-10** — machine check evaluated and rejected: a statistic scan found 83 candidate hits, most of them advice numbers (pricing buffers, format specs, cadence guidance) that a regex cannot distinguish from claims about the world, so real findings would drown in false positives. The expectation is instead codified as CONVENTIONS § Evidence discipline (inline citation or explicit directional marker, advice numbers exempt) and stays enforced by prose review |
| 10 | `VOICE_PROFILE.md` ships as a skeleton the owner must fill. If it stays empty, content skills fall back to generic human-voice defaults and the artifact adds a step without adding value. Should an empty profile be a warning in `@biz-bootstrap status` and the content skills' context summary? | Medium | framework | **Resolved 2026-08-10** — yes: skeleton profiles (unfilled `<` placeholders) are treated as absent and warned about in all four content skills' context summaries and in `@biz-bootstrap status`. Additionally CONVENTIONS § Voice loop now makes VOICE_RETRO entries mandatory per publish (previously voluntary), and `@biz-review weekly` checks retro coverage |
| 11 | Should the mirrored skeletons be deduped into shared referenced docs — biz-content/biz-youtube (~150 duplicated lines: tracker load, topic buckets, 30-day challenge, status template) and biz-writing/biz-social (~50-line duplicated I0 context contract)? Skills ship standalone to target projects (fat-client copies `skills/`, thin-client resolves from source), so dedup trades self-containment for brevity; framework-verify must keep failing if a shared doc loses its references | Low | framework | Open — deferred 2026-08-19 (owner chose low-risk dedup only); revisit with the full-dedup refactor |
| 12 | Does `@biz-advisory` output clear its own calibration bar (the 2026-08-20 rescue-team feedback case: reframe, ranked options with precedent, anti-advice, decisive recommendation) when run cold by a fresh agent on a consumer project? If not, which protocol step collapsed — reframe pass, evidence pass, or decisiveness? | Medium | framework | Open — validate on `future-strategy` (the case's home repo) |
| 13 | Is the `biz-sources` seed catalog (Dunford, Enns, Stark, patio11, Indie Hackers, First Round Review, Lenny's, fractional-services marketplaces) the right starter set for this operator's actual decisions, and does the vetting step hold up in practice (sources fetched, freshness dated, bias noted) rather than being cataloged from memory? | Medium | framework | Open — first `@biz-sources find` run on a live project is the test |
