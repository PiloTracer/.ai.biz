# Business OS Framework Audit — 2026-08-14

**What:** Structural audit of the `.ai.biz` (Business OS) framework repo per `.work.biz/prompts/framework-audit.md` (8 checks), verifying clarity-contract adoption and sweeping for drift.
**Status:** Approved — 2026-08-14
**Needs:** review of 2 confirmed findings and the proposed fix list; no fixes applied.

## Scope

Repo `/mnt/work/Projects/.ai.biz`, read-only except this report. Checks: (1) registry congruence, (2) Operator handoff adoption, (3) Document clarity adoption, (4) routing docs, (5) templates ↔ `.cursorrules` sync, (6) hooks, (7) protected surfaces, (8) verifiers. Adoption checks were grep-verified against all 23 `skill.md` files and all `templates/work/` templates, not taken from the prior-work summary.

## Check results

### 1. Registry congruence — PASS

- 23 skill dirs under `skills/`; every one has `skill.md` with `name:` matching the folder (loop over `skills/biz-*/`, 23× OK).
- `.cursorrules:143-167` skills table = 23 rows; `skills/README.md:25-49` registered-skills table = 23 rows; identical id sets.
- `skills/SKILL_DEPENDENCIES.md:5-63` (gate graph + system skills + biz-director) mentions all 23 ids.
- `scripts/framework-verify.sh:58-62` enforces `.cursorrules` ↔ `skills/README.md` congruence; exit 0.

### 2. Operator handoff adoption — PASS

- Contract present: `skills/SKILL_DEPENDENCIES.md:110` (`## Operator handoff contract`).
- All 23 `skills/<id>/skill.md` reference it (grep, 23/23); enforcement at `scripts/framework-verify.sh:74-80`, exit 0.
- Template-close spot checks (5 skills): `skills/biz-strategy/skill.md:384` (Form A line inside the reconcile report template), `skills/biz-review/skill.md:214-216`, `skills/biz-content/skill.md:315`, `skills/biz-discovery/skill.md:218`, `skills/biz-session/skill.md:229-231,406-408,454-456,491-493,558-560` (Form B blocks with `**Needs your approval:**` / `**Needs your answer:**` / `**Next step:**` embedded in the report templates themselves).
- Every skill.md contains ≥1 `**Next step:**` (grep count, min 1 — biz-content, biz-deploy-basic, biz-deploy-files, biz-objections, biz-products, biz-strategy, biz-youtube).

### 3. Document clarity adoption — PASS with 1 finding

- Contract present: `skills/SKILL_DEPENDENCIES.md:140` (`## Document clarity contract`).
- All 13 doc-generating skills reference it (grep): biz-strategy:94, biz-brand:473, biz-pricing:243, biz-proposal:62, biz-review:212, biz-writing:358, biz-ideas:252, biz-products:273, biz-content:316, biz-social:735, biz-youtube:441, biz-market-validate:99, biz-pipeline-diagnosis:128. Matches the enforced list at `scripts/framework-verify.sh:90-95`.
- Updated doc templates: 7 of 8 carry a Status/Needs header (first 6 lines) and exactly one `## Next action`: `templates/work/plans/UNKNOWNS.md.template`, `templates/work/context/HANDOFF.md.template`, `templates/work/research/research_index.md.template`, `templates/work/reference/OPERATOR_PROFILE.md.template`, `templates/work/reference/VOICE_PROFILE.md.template`, `templates/work/reference/VOICE_RETRO.md.template`, `templates/work/reference/PROJECTS.md.template`.
- **Finding F1 (confirmed):** `templates/work/plans/NEXT.md.template` has the Status/Needs header but **zero** `## Next action` headings; it closes with `## Recommended next` and `## Active tasks` instead. Contract §5 (`skills/SKILL_DEPENDENCIES.md:158-161`) requires exactly one `## Next action` or a `Next action: none — <reason>` line.
- Not-updated classification is defensible: `templates/work/gates.md.template` is machine-read (`scripts/gate-verify.sh`; reading rule at `skills/SKILL_DEPENDENCIES.md:71`); pipeline trackers, `*_STATUS` templates, README templates, and `touch-scope.template` are ledgers/trackers/navigation, not narrative docs. None would be mistaken for a narrative document. Note: `platform-tracker.md.template` gets its Status/Needs header and `## Next action` at instantiation time via instruction in `skills/biz-youtube/skill.md:302` — works when the skill drives creation; a manual copy would skip them (inference, low risk).

### 4. Routing docs — PASS

- `START_HERE.md`, `PROCESS_ROUTER.md`, `README.md`: every repo-relative path reference and every `@biz-*` skill reference resolves to an existing file or `skills/<id>/skill.md`. Zero broken references.
- Bootstrap-time references that do not exist in this repo but are generated in targets: `.work.biz/gates.md` (`README.md:36,73,220`; `START_HERE.md:67,68,165,185`) from `templates/work/gates.md.template`; `.work.biz/pipeline/outreach-cadence.md` (`README.md:80`) from its template. Both templates exist; docs describe them as bootstrap outputs. Not broken.
- Accepted non-local handles: `@x-director` (sibling `.ai` framework, e.g. `START_HERE.md:70`, `PROCESS_ROUTER.md:5`, `README.md:225`); `@process-router` (`PROCESS_ROUTER.md:10-14`) is a self-reference to the doc, not a registered skill.

### 5. Templates ↔ .cursorrules sync — PASS

- `diff .cursorrules templates/cursorrules.template` → identical (0 lines differ), including the Communication closing requirement at `.cursorrules:243-247` == `templates/cursorrules.template:243-247` and identical 23-row skills tables.
- Paths referenced in `.cursorrules` resolve: `START_HERE.md:60`, `CONVENTIONS.md:69`, `scripts/touch-scope-verify.sh`, `scripts/blast-radius-check.sh`, `scripts/gate-verify.sh`, `scripts/framework-verify.sh` (`.cursorrules:101-104`), `templates/work/touch-scope.template` (`.cursorrules:106`), layout dirs `skills/`, `standards/`, `templates/`, `.work.biz/` (`.cursorrules:267-274`).
- 7 `REPLACE:` tokens remain in `.cursorrules` (reported by `biz-cursorrules-verify`) — expected for the source repo; operators fill them at bootstrap.

### 6. Hooks — PASS

- All 4 mandated hooks present and executable: `hooks/prepare-commit-msg`, `hooks/commit-msg`, `hooks/pre-commit`, `hooks/post-commit` (mode 0775).
- `scripts/install-git-hooks.sh:42` installs exactly those 4; no references to removed scripts. `--self-test` supported (`install-git-hooks.sh:12`).
- `hooks/pre-commit:22` invokes `touch-scope-verify.sh` and `blast-radius-check.sh` — both exist. `hooks/prepare-commit-msg:16` reads `.work.biz/active-ref` (runtime file, guarded by existence check). No stale references found; every `scripts/*.sh` referenced anywhere in the repo exists.

### 7. Protected surfaces — PASS with 1 observation

- Single protected list: `.cursorrules:173-185` (`package.json`, `docker-compose*.yml`, `Dockerfile.*`, `.env*`), identical in `templates/cursorrules.template:173-180`. No protected-files JSON exists; no embedded protected lists in `scripts/touch-scope-verify.sh` (reads `.work.biz/touch-scope` — a scope declaration, not a protected list) or `scripts/blast-radius-check.sh` (classifies by top-level area). No cross-list skew possible.
- **Observation (inference):** the protected list is generic app-project surface; framework high-blast paths (`skills/SKILL_DEPENDENCIES.md`, `standards/`, `hooks/`, `scripts/framework-verify.sh`) are not listed as protected anywhere. They are indirectly guarded by `framework-verify.sh` and the pre-commit gate, so exposure is low.

### 8. Verifiers — PASS with 1 finding

| Verifier | Invocation | Exit | Result |
|----------|-----------|------|--------|
| `scripts/framework-verify.sh` | bare | 0 | "framework-verify: all checks passed" (includes its own biz-cursorrules-verify repair self-test) |
| `scripts/gate-verify.sh` | `--self-test` | 0 | "gate-verify self-test: PASS" |
| `scripts/biz-cursorrules-verify.sh` | bare | 2 | usage error (no `--self-test`) |
| `scripts/biz-cursorrules-verify.sh` | `--fat .` | 1 | FAIL: expects `<target>/.ai.biz/skills/` (`biz-cursorrules-verify.sh` targets deployed projects, not the source repo) |

- **Finding F2 (confirmed):** `biz-cursorrules-verify.sh` has no `--self-test` mode and cannot produce a passing run against this source repo — with `--fat .` it reports "local .ai.biz/skills/: missing — broken fat-client copy" because it treats the target as a deployed project. It is exercised indirectly inside `framework-verify.sh` ("OK: biz-cursorrules-verify --fix repairs stale pointer"), so the gap is operator-facing ergonomics, not coverage.

## Findings

| # | Classification | Finding | Evidence |
|---|---------------|---------|----------|
| F1 | confirmed | `templates/work/plans/NEXT.md.template` lacks the required `## Next action` (closes with `## Recommended next` / `## Active tasks`) | `templates/work/plans/NEXT.md.template` (0× `^## Next action`); contract `skills/SKILL_DEPENDENCIES.md:158-161` |
| F2 | confirmed | `biz-cursorrules-verify.sh` has no self-test and fails (exit 1) when run against the source repo | run log: exit 2 bare, exit 1 `--fat .` |
| F3 | inference | F1 may be intentional — NEXT.md *is* the next-action tracker, so a closing `## Next action` is arguably redundant; the contract text does not exempt it | contract `skills/SKILL_DEPENDENCIES.md:158-161` |
| F4 | inference | Protected-files list does not cover framework-critical surfaces; risk is low because verifiers + pre-commit gate guard them | `.cursorrules:173-185`; `hooks/pre-commit:22` |
| F5 | inference | `platform-tracker.md.template` acquires header/`## Next action` only via skill-time instruction; manual instantiation skips them | `skills/biz-youtube/skill.md:302` |
| — | unknown | none | |

## Proposed fixes (NOT applied)

1. F1: append a one-line closing `## Next action` section to `templates/work/plans/NEXT.md.template` (e.g. pointing at the first row of `## Recommended next`), or add an explicit exemption for the NEXT doc in contract §5.
2. F2: add a `--self-test` mode to `scripts/biz-cursorrules-verify.sh` (mirroring `gate-verify.sh` / `blast-radius-check.sh`), or document that it is target-only and exercised via `framework-verify.sh`.
3. F4 (optional): extend the Protected Files list in `.cursorrules` + `templates/cursorrules.template` with framework-critical paths, or state explicitly that they are guarded by the verifier suite.

## Next action

Run `@biz-session close` after operator reviews findings F1/F2 and approves or rejects proposed fixes 1–3.
